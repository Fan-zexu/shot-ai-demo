# MVP 验证说明

验证分为两层：自动化工程证据回答“系统是否按契约工作”，真实样本实验回答“受控投篮视频上的精度和表达是否达到目标”。前者不能替代后者。

## 自动化工程证据

在干净工作区执行：

```bash
pnpm install --frozen-lockfile
python3.11 -m venv .venv
.venv/bin/pip install -r services/pose-worker/requirements.lock
pnpm verify
```

| 范围 | 自动检查 | 通过标准 |
| --- | --- | --- |
| 公共契约 | TypeBox 闭合结构、六事件顺序、Python 夹具互认 | 全部通过，无未知字段 |
| 视频与姿态 | 探测、时间轴、视角、质量门控、事件、归一化、预览、安全路径 | Python 测试零失败 |
| 比较引擎 | 兼容性、事件硬锚点、阶段内受限 DTW、低置信度、差异窗口 | TypeScript 测试零失败 |
| API | 文件哈希/Range、事务状态、恢复、重试、软删除、报告与导出 | 集成测试零失败 |
| H5 | 共享播放状态、六事件、低置信度不高亮、40ms 校正规则 | Vitest 零失败 |
| 浏览器 | 三模式共享状态、320px 双列/44px 控件、桌面调试证据 | Chromium 与 WebKit 零失败 |
| 构建 | 严格 TypeScript 与 Vite production build | 零错误 |

E2E 通过网络拦截注入确定性 `ReportBundle` 和调试摘要，只证明前端对公共数据的消费一致，不证明 MediaPipe 对真实投篮的精度。

## 真实视频实验：正在验证

下表是必须补录的实验批次，不得用单元测试或合成姿态结果填写。样本文件保持在 Git 外，使用 `fixtures/manifest.example.json` 的格式记录 SHA-256、投篮手、视角、缺陷标签和人工事件标注。

| 数据集 | 计划数量 | 当前数量 | 当前状态 | 目的 |
| --- | ---: | ---: | --- | --- |
| 参考模板 | 1–3 | 2 | 进行中 | 模板软门禁、姿态产物与真实上传链路 |
| 有效用户视频 | 5–8 | 1 | 进行中 | 完整分析、报告生成和交互理解 |
| 边界用户视频 | 4–6 | 0 | 未执行 | 验证质量提示覆盖，以及技术硬阻断不会伪造报告 |

### 2026-07-16 模板冒烟批次

两段文件均保持在 Git 外，通过公开模板 API 上传，并完成 `validating_file → extracting_pose → validating_pose_quality → detecting_events → normalizing_motion → writing_artifact → ready` 全链路。该结果只证明当前样本能够生成模板，不代表六事件精度已经通过人工标注验收。

| 样本 | SHA-256 | 视频摘要 | 结果 | 质量提示 |
| --- | --- | --- | --- | --- |
| 安东尼.mov | `4ced283a481a1db4dfe67dee61900d46977aed82a86325371477cb5a562b3446` | 1.967s、914×1394、60fps、右手 | `ready` | 模板速度不可信，不比较真实速度与节奏 |
| 乔治.mov | `3eb278cce7e80566febc9351223a0d7cf7a39dc26152185e48a440c4109d3536` | 3.917s、914×1394、60fps、右手 | `ready` | 模板速度不可信，不比较真实速度与节奏 |

### 2026-07-16 用户视频纵切冒烟

`用户投篮 1.mp4` 保持在 Git 外，人工检查为单人、固定机位、右手侧面、全身持续入镜并包含一次完整投篮。旧门禁只因帧率低于 30fps 拒绝；软门禁启用后，同一源文件完整通过 `validating_user → extracting_user_pose → detecting_user_events → checking_compatibility → aligning_phases → computing_differences → generating_previews → building_report → ready`。

| 样本 | SHA-256 | 视频摘要 | 参考模板 | 质量结果 | 最终结果 |
| --- | --- | --- | --- | --- | --- |
| 用户投篮 1.mp4 | `3f24075d77375b39d9993837dc2369bb8519a2eca550e1270e68cc6e8adf5867` | 5.085s、1254×720、23.803fps、右手 | 安东尼 | `accepted`；帧率不足和速度未确认为 `warning` | `cmp_4dd13d66a4554459850203da96be471f` / `result_e9c11e86e1974e6d998654c5376b590c` / `ready` |

报告保存 5 个动作阶段、114 个同步采样和 9 个差异窗口。共同可比较区域为下肢、躯干、投篮手臂和全身时序；引导手因覆盖不足统一标记为不可比较。由于模板和用户视频速度都未经确认，结果阈值快照中的速度权重为 `0`，不输出真实速度、节奏或阶段耗时差异。

本次记录证明这一个真实样本能够走通，不代表 5–8 个有效用户视频的计划已经完成，也不代表六事件相对人工标注的精度已经达标。

### 算法与一致性记录

| 指标 | 目标 | 实测 | 状态 |
| --- | ---: | ---: | --- |
| 六事件相对人工标注中位绝对误差（30fps） | ≤ 4 帧 | 待测 | 未执行 |
| 同一输入连续 3 次事件帧最大偏差 | ≤ 1 帧 | 待测 | 未执行 |
| DTW 事件顺序错误 | 0 | 0 / 1 份真实报告 | 进行中 |
| 低置信度区域误高亮 | 0 | 待测 | 未执行 |
| 三模式当前模板/用户帧映射不一致 | 0 | 待测 | 未执行 |
| 有效输入生成报告失败 | 0 | 0 / 1 份真实输入 | 进行中 |
| 无效输入错误生成报告 | 0 | 待测 | 未执行 |

### 2026-07-17 显示时钟回归与真实报告 A/B

比较引擎保留完整 `renderTimeline` 作为阶段对齐证据，新增独立 `displayTimeline` 驱动 30 FPS 预览和三模式播放。30 / 60 / 120 FPS 的同一段两秒测试动作都输出 60 帧、2000 ms，时长误差为 0 ms。

| 样本 | 旧报告 | 新报告 | 对齐样本 | 旧预览 | 新预览 |
| --- | --- | --- | ---: | ---: | ---: |
| 主要问题样本 | `cmp_50e4923141f44d94b4a7e68d0cc74b15` | `cmp_129eb2c16cce4d66ae908dcc30265e0e` | 144 | 4800 ms | 1133.333 ms |
| 相近素材样本 | `cmp_dc88950283b742e1b9e210d8a45cbb9c` | `cmp_37a01e925e4c4da09af91a823b68e550` | 154 | 5133.333 ms | 1133.333 ms |

旧报告的结果哈希和预览文件引用保持不变；新报告使用算法版本 `1.1.0`。浏览器检查确认新预览媒体时长均为 `1.133333` 秒、两侧在“标准对齐速度”下同步播放，页面不再静默自动降速。截图和完整解释见 [`report-interaction-handoff-2026-07-16.md`](./report-interaction-handoff-2026-07-16.md)。

### 2026-07-17 核心关节显示回归与真实页面检查

动作产物和报告数据继续保留 MediaPipe 33 点；用户界面的三种报告模式默认统一绘制 17 个核心关节，完整点位只由桌面调试开关显式开启。前端测试夹具保留眼部、嘴部和手指点，自动化检查确认这些点默认不进入任何一种用户视图。

真实页面使用 `cmp_129eb2c16cce4d66ae908dcc30265e0e` 检查：默认并排模式实际出现 17 种核心关节，`left_eye` 和 `right_index` 均为 0；切换骨架叠加和动作通道后仍没有非核心点。打开“显示完整 33 点”后，页面实际出现完整 33 种 MediaPipe 点位。置信度低于 `0.35` 的圆点继续不绘制，因此每层总圆点数可能少于 33，但数据仍可在调试证据中查看。

证据截图见 [`report-interaction-handoff-2026-07-16.md`](./report-interaction-handoff-2026-07-16.md) 的 3.6 节。该验收只证明展示层降低了点簇噪声，不代表姿态识别精度提升。

### 2026-07-17 连续播放、诊断与不可变锚点检查

三种报告模式改为共享一个基于 `requestAnimationFrame` 的经过时间主时钟。播放态在相邻显示采样间插值，并使用按置信度约束的低延迟展示平滑；暂停、拖动、六事件跳转、调试表格和导出仍读取原始 `renderFrames` 与 `renderTimeline`。

自动化覆盖重复映射统计、可见核心关节峰值跳变、中间帧插值、输入对象不可变和六事件锚点不被平滑；Web 单元测试 20 项通过。Chromium 与 WebKit 端到端测试确认模式切换继续共享同一时钟，播放态标记为 `smoothed-interpolated-copy`，暂停和事件跳转恢复 `raw-analysis-frame`，共 8 项通过。

| 报告 | 对齐重复映射（模板 / 用户） | 原始峰值跳变（模板 / 用户） | 60 FPS 展示峰值（模板 / 用户） |
| --- | ---: | ---: | ---: |
| `cmp_129eb2c16cce4d66ae908dcc30265e0e` | 4.2% / 7.7% | 0.446 / 0.428 | 0.192 / 0.159 |
| `cmp_37a01e925e4c4da09af91a823b68e550` | 10.5% / 11.1% | 0.457 / 0.548 | 0.225 / 0.278 |

峰值使用归一化核心关节的相邻显示位移，只能比较展示前后稳定性。当前 120 Hz 桌面浏览器实测 120 次提交/秒；“释放姿态代理”在真实报告 `cmp_129e...` 中仍为原始 Sample 142。截图和诊断边界见交接文档 3.7 与 P0-4。

### 2026-07-17 稳定全动作 fit-to-view 检查

骨架叠加和动作通道现在共享一个由整份报告核心关节联合边界计算出的固定变换；最大通道半径、描边宽度和 24px 安全区都进入边界计算。低于 `0.35` 的离群点不会触发缩放变化，分离布局两侧继续使用相同尺度。

单元测试覆盖所有帧的核心关节与通道描边不越界、离群点过滤和分离布局；组件与浏览器测试确认事件跳转前后变换恒定，且叠加/通道使用同一 `scale/center`。Web 单元测试 24 项、Chromium 与 WebKit 端到端测试 10 项通过。

真实报告 `cmp_129eb2c16cce4d66ae908dcc30265e0e` 实测固定变换为 `scale=83.305`、`center=208.367,209.245`。释放姿态代理帧可见元素边界为 `x=150.20–289.59`、`y=26.86–367.76`；动作通道 18px 描边完整入镜。截图见交接文档 3.8。

### 2026-07-17 跨骨架差异连线移除检查

骨架叠加不再绘制模板到用户的跨骨架虚线；身体区域高亮和“当前区域证据”的角度、位置、阶段与置信度保持不变。组件和浏览器回归确认：存在高亮差异时 `.difference-link` 数量仍为 0。

真实报告 `cmp_129eb2c16cce4d66ae908dcc30265e0e` 的释放姿态代理帧实测跨骨架连线为 0，区域高亮元素为 35。该修改只降低播放噪声，不改变比较结果。截图见交接文档 3.9。

### 2026-07-17 拍摄适配度与模式降级检查

报告 API 根据模板与用户 MotionArtifact 已有的侧面视角、全身覆盖、连续缺失、姿态置信度和相机稳定性检查生成三档展示可用性，不写回不可变比较结果：基础可叠加、仅供参考、仅并排可用。非侧面、严重出框或严重姿态不稳会禁用叠加和通道，但并排视频始终保留。

API 判定测试覆盖三档边界和可解释原因；Web 单元测试 27 项、Chromium 与 WebKit 端到端测试 12 项通过。真实 API 检查结果为：`cmp_129e...` 因模板相机不稳定降为 `reference_only`；`cmp_37a...` 因模板和用户相机不稳定降为 `reference_only`。真实页面显示“仅供参考”，两个候选模式仍可打开且没有被伪装成精确叠加。截图见交接文档 3.10。

### 2026-07-17 报告信息层级检查

报告首屏不再展示同步样本、差异窗口、可比较区域、Sample 编号或两侧帧号。当前阶段与一个优先观察区域先于动作画面出现；区域卡片只描述是否触发模板差异高亮。完整样本数、帧映射、角度、位置、阶段和置信度保留在报告末端默认收起的“技术证据”中。

Web 单元测试 28 项覆盖首屏层级、观察重点、技术证据默认收起及展开可访问性；Chromium 与 WebKit 共 12 项端到端测试覆盖共享状态和 320px 无横向溢出。真实报告 `cmp_129eb2c16cce4d66ae908dcc30265e0e` 初始显示“准备 → 最低点 / 下肢”；展开技术证据后仍可读取 144 个同步采样、8 个差异窗口、4 / 5 个可比较区域和 `TEMPLATE F94 · USER F113`。所有区域文案只描述与模板的差异，并明确不代表动作错误或训练建议。截图见交接文档 3.11。

### 人工理解记录

每位测试者分别使用并排视频、骨架叠加、动作通道后记录：能否指出主要差异、首次理解耗时、是否误把差异当成错误、是否理解“释放姿态代理”和“参考动作通道”的边界、是否依赖桌面调试数据。MVP 不据此输出评分或训练建议。

## 放行条件

工程 Demo 只有在以下证据同时成立时才可称为通过真实链路验收：

1. `pnpm verify` 在干净环境零失败。
2. 上述真实样本数量达到计划下限，文件哈希和人工标注可追溯。
3. 所有硬指标达标；任何真实离手、评分、诊断或训练收益表述仍保持禁用。
4. 任一报告能从数据库记录追溯到源文件哈希、两份动作产物、模型/算法/阈值版本和任务耗时。

当前已完成自动化工程验证、两个真实模板冒烟和一个真实用户视频纵切；用户视频数量、边界样本、人工事件标注和完整指标尚未达到真实链路放行条件。
